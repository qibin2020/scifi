`timescale 1 ns / 1 ps

// VOID skeleton — both port list and body must be implemented from scratch.
// The companion C++ binder (sim/stream_wrapper_binder.cc) is also empty.
// You design BOTH the .v module port interface AND the C++ binder, and you
// connect them through the Verilator-generated Vstream_wrapper class.
//
// The task description (top.md) specifies the required external contract:
// the C function signature, the input/output bit widths, the dataflow rules.
// Everything between python (verify_golden.py) and your .v module is up to you.

module stream_wrapper;
endmodule
