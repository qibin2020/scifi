I want to benchmark three sorting algorithms (bubble sort, merge sort, quicksort)
on random arrays of size 1000, 10000, and 100000. Time each one, write results
to a CSV, and then create a summary showing which algorithm wins at each size.
The summary should clearly state the fastest algorithm for each array size.
